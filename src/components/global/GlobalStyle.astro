---
import Theme from "./Theme.astro";
import Reset from "./Reset.astro";
---
<Theme/>
<Reset/>


<style is:global lang="scss">
  @font-face {
    font-family  : "UDEVGothicJPDOC";
    src          : url("/font/UDEVGothic_v1-3-1/UDEVGothicJPDOC-Bold.ttf") format("truetype");
    font-weight  : normal;
    font-style   : normal;
    font-display : swap;
  }

  $minWidth    : 400px;
  $bigWidth    : 1300px;
  $middleWidth : 800px;
  $smallWidth  : 600px;
  @mixin responsive($prop,$def,$big,$middle,$small) {
    $prop : $def;
    @media (max-width : $bigWidth) {
      $prop : $big;
    }
    @media (max-width : $middleWidth) {
      $prop : $middle;
    }
    @media (max-width : $smallWidth) {
      $prop : $small;
    }
  }
</style>